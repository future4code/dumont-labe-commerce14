import React from 'react';
import styled from "styled-components";
import {ProductCard} from './ProductCard'

const ProductsContainer = styled.div`
`;
  export class Products extends React.Component {
      
render(){
   return (
    <div>
        const produtos =this.listaProdutos.map[
        {<img src= {this.listaProdutos.imageUrl}></img>
         <p>{this.listaProdutos.name}</p>
         <p>R${this.listaProdutos.value}</p>}
      ]
    <button onClick={this.adicionarCarrinho} className="botao">Adicionar ao carrinho</button>
    </div>
   )
  }
}